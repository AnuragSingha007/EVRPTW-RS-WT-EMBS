[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"

[project]
name = "evrptw-rs-wt-embs"
version = "0.1.0"
description = "Utilities for modelling EV routing energy events"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
test = ["pytest>=7.0"]
